{
  "diagram": {
    "width": 460,
    "height": 320,
    "nodes": [
      { "id": "toolCall", "label": "Session event\ntool/call", "x": 200, "y": 15, "width": 180, "height": 36 },
      { "id": "pre", "label": "tools/pre-execute\nhooks·perm·sandbox", "x": 200, "y": 83, "width": 210, "height": 40 },
      { "id": "guards", "label": "monotonic guards", "x": 200, "y": 151, "width": 190, "height": 36 },
      { "id": "around", "label": "tools/execute\nwaterfall", "x": 200, "y": 219, "width": 190, "height": 36 },
      { "id": "toolBody", "label": "tool execute()\nbody", "x": 200, "y": 283, "width": 180, "height": 36 },
      { "id": "approval", "label": "ctx.approval\none-shot prompt", "x": 385, "y": 117, "width": 140, "height": 40 },
      { "id": "denied", "label": "denied", "x": 62, "y": 151, "width": 110, "height": 36 }
    ],
    "edges": [
      { "from": "toolCall", "to": "pre" },
      { "from": "pre", "to": "guards", "label": "allow" },
      { "from": "pre", "to": "denied", "label": "deny" },
      { "from": "pre", "to": "approval", "label": "ask" },
      { "from": "approval", "to": "guards", "label": "allowed-once" },
      { "from": "approval", "to": "denied", "label": "rej/cancel/unavail" },
      { "from": "guards", "to": "around", "label": "allow" },
      { "from": "guards", "to": "denied", "label": "deny" },
      { "from": "around", "to": "toolBody" }
    ],
    "steps": [
      { "activeNodeIds": ["toolCall"], "activeEdgeIds": [], "title": "Log the call", "desc": "A tool/call session event is logged before execution begins." },
      { "activeNodeIds": ["pre"], "activeEdgeIds": ["toolCall->pre"], "title": "Pre-execute gate", "desc": "tools/pre-execute resolves allow, deny, or ask for the call." },
      { "activeNodeIds": ["approval"], "activeEdgeIds": ["pre->approval"], "title": "Ask the human", "desc": "An ask dispatches the ctx.approval one-shot waterfall to an answerer." },
      { "activeNodeIds": ["approval", "denied"], "activeEdgeIds": ["approval->guards", "approval->denied", "pre->denied"], "title": "Allow or deny", "desc": "allowed-once flows to guards; rejected, cancelled, or unavailable deny." },
      { "activeNodeIds": ["guards"], "activeEdgeIds": ["pre->guards", "guards->around"], "title": "Monotonic guards", "desc": "Guards that allow pass the call on to the tools/execute waterfall." },
      { "activeNodeIds": ["around", "toolBody"], "activeEdgeIds": ["guards->around", "around->toolBody"], "title": "Run the body", "desc": "The wrapped tools/execute waterfall finally invokes the tool's execute() body." }
    ]
  }
}
